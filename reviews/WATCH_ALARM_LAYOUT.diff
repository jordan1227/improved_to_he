--- deployed/gamedata\shaders\r3\model_blood_icon.ps
+++ source/gamedata\shaders\r3\model_blood_icon.ps
@@ -44,7 +44,6 @@
 	t_base.xyz = lerp(watchBgColor(), t_base.xyz, brightness);
 
 	//Output
-	t_base.xyz *= lerp(1.0, 0.35, saturate(m_he_watch_alarm.y));
 
 	return float4(t_base.xyz, 1.0);
 }--- deployed/gamedata\shaders\r3\model_compass_icon.ps
+++ source/gamedata\shaders\r3\model_compass_icon.ps
@@ -60,7 +60,6 @@
 	t_base.rgb = lerp(t_base.rgb, mark_rgb, coverage * available);
 
 	// Alarm editor dimming.
-	t_base.rgb *= lerp(1.0, 0.35, saturate(m_he_watch_alarm.y));
 
 	float brightness = 1.f;
 	brightness *= calc_glitch_brightness_watch_ui(brightness);
--- deployed/gamedata\shaders\r3\model_healthbar.ps
+++ source/gamedata\shaders\r3\model_healthbar.ps
@@ -37,7 +37,6 @@
     t_base.xyz = lerp(watchBgColor(), t_base.xyz, brightness);
     
     //Output
-    t_base.xyz *= lerp(1.0, 0.35, saturate(m_he_watch_alarm.y));
 
     return float4(t_base.xyz, 1.0);
 }--- deployed/gamedata\shaders\r3\model_health_icon.ps
+++ source/gamedata\shaders\r3\model_health_icon.ps
@@ -42,7 +42,6 @@
 	t_base.xyz = lerp(watchBgColor(), t_base.xyz, brightness);;
 
     //Output
-    t_base.xyz *= lerp(1.0, 0.35, saturate(m_he_watch_alarm.y));
 
     return float4(t_base.xyz, 1.0);
 }--- deployed/gamedata\shaders\r3\model_powerbar.ps
+++ source/gamedata\shaders\r3\model_powerbar.ps
@@ -37,7 +37,6 @@
     t_base.xyz = lerp(watchBgColor(), t_base.xyz, brightness);
 
     //Output
-    t_base.xyz *= lerp(1.0, 0.35, saturate(m_he_watch_alarm.y));
 
     return float4(t_base.xyz, 1.0);
 }--- deployed/gamedata\shaders\r3\model_power_icon.ps
+++ source/gamedata\shaders\r3\model_power_icon.ps
@@ -41,7 +41,6 @@
 	t_base.xyz = lerp(watchBgColor(), t_base.xyz, brightness);
 
     //Output
-    t_base.xyz *= lerp(1.0, 0.35, saturate(m_he_watch_alarm.y));
 
     return float4(t_base.xyz, 1.0);
 }--- deployed/gamedata\shaders\r3\model_satiety_icon.ps
+++ source/gamedata\shaders\r3\model_satiety_icon.ps
@@ -45,7 +45,6 @@
 	t_base.xyz = lerp(watchBgColor(), t_base.xyz, brightness);
 
 	//Output
-	t_base.xyz *= lerp(1.0, 0.35, saturate(m_he_watch_alarm.y));
 
 	return float4(t_base.xyz, 1.0);
 }--- deployed/gamedata\shaders\r3\model_thirst_icon.ps
+++ source/gamedata\shaders\r3\model_thirst_icon.ps
@@ -44,7 +44,6 @@
 	t_base.xyz = lerp(watchBgColor(), t_base.xyz, brightness);
 
 	//Output
-	t_base.xyz *= lerp(1.0, 0.35, saturate(m_he_watch_alarm.y));
 
 	return float4(t_base.xyz, 1.0);
 }--- deployed/gamedata/scripts/ogsr_shaders_control.script
+++ source/gamedata/scripts/ogsr_shaders_control.script
@@ -135,12 +135,14 @@
 	if fraction < 0 then fraction = 0 elseif fraction > 1 then fraction = 1 end
 	shader_set_custom_param("m_he_watch_ring", fraction, available, 0, 0)
 
-	-- Alarm contract: x = enabled, y = watch alarm editor active.
+	-- Alarm contract: x = enabled, y = editor active, z/w = saved hour/minute.
 	local enabled, editing = 0, 0
 	local cfg = vergas_watch and vergas_watch.g_watch and vergas_watch.g_watch:alarm_config()
 	if cfg and cfg.f then enabled = 1 end
 	if he_watch_ui and he_watch_ui.is_active and he_watch_ui.is_active() then editing = 1 end
-	shader_set_custom_param("m_he_watch_alarm", enabled, editing, 0, 0)
+	local alarm_h = cfg and math.floor(tonumber(cfg.h) or 0) % 24 or 0
+	local alarm_m = cfg and math.floor(tonumber(cfg.m) or 0) % 60 or 0
+	shader_set_custom_param("m_he_watch_alarm", enabled, editing, alarm_h, alarm_m)
 end
 
 local function on_key(key, bind)
--- deployed/gamedata/shaders/r3/he_watch_common.h
+++ source/gamedata/shaders/r3/he_watch_common.h
@@ -4,7 +4,7 @@
 */
 
 uniform float4 m_he_watch_ring;   // x = radiation fill 0..1, y = ring available 1/0, z/w reserved
-uniform float4 m_he_watch_alarm;  // x = alarm enabled 1/0, y = editor active 1/0, z/w reserved
+uniform float4 m_he_watch_alarm;  // x = alarm enabled 1/0, y = editor active 1/0, z = saved alarm hour, w = saved alarm minute
 
 float he_watch_arc(float progress, float fraction)
 {
--- deployed/gamedata/shaders/r3/model_clock.ps
+++ source/gamedata/shaders/r3/model_clock.ps
@@ -56,6 +56,10 @@
 	tc.x *= 1.778;
     tc += 0.5;	
 
+    // Reserve the bottom strip for saved alarm time; preserve the top margin.
+    if (m_he_watch_alarm.x > 0.5)
+        tc.y = (I.tc0.y - 0.10) / 0.65;
+
     //Skew it
     // tc.x += tc.y * 0.1;
     // tc.x -= 0.05;
@@ -109,17 +113,30 @@
 	//Custom blend ffs
 	color = color * (1.0 - watch_color.w) + watch_color.xyz * watch_color.a;
 
-	// [HE-PORT] Alarm-enabled marker. icon_alarm alpha is the silhouette, tinted to a
-	// light watch-display color. Placement rect is a first-pass proposal to verify.
-	float2 alarm_min = float2(0.85, 0.03);
-	float2 alarm_max = float2(0.95, 0.13);
-	float2 alarm_uv = (I.tc0 - alarm_min) / (alarm_max - alarm_min);
-	float alarm_mask = 0.0;
-	if (alarm_uv.x >= 0.0 && alarm_uv.x <= 1.0 && alarm_uv.y >= 0.0 && alarm_uv.y <= 1.0)
-		alarm_mask = s_he_alarm.Sample(smp_base, alarm_uv).a;
-	float alarm_on = step(0.5, m_he_watch_alarm.x);
-	float3 alarm_tint = float3(1.0, 0.88, 0.55);
-	color = lerp(color, alarm_tint, saturate(alarm_mask) * alarm_on);
+	// Saved-alarm footer. Model UV metric: width/height = 1.83288.
+	// Keep it inside the lower rectangular portion of the clipped clock mesh.
+	if (m_he_watch_alarm.x > 0.5)
+	{
+		int ah = int(floor(m_he_watch_alarm.z + 0.5));
+		int am = int(floor(m_he_watch_alarm.w + 0.5));
+		float2 uv = I.tc0;
+		float4 saved = float4(0, 0, 0, 0);
+		// Square glyph cells in physical face space, with tighter colon spacing.
+		float2 cell = float2(0.09821, 0.18);
+		saved += sample_digit((uv - float2(0.46, 0.80)) / cell, ah / 10, 0);
+		saved += sample_digit((uv - float2(0.54, 0.80)) / cell, ah % 10, 0);
+		saved += sample_digit((uv - float2(0.60, 0.80)) / cell, 10, 0);
+		saved += sample_digit((uv - float2(0.66, 0.80)) / cell, am / 10, 0);
+		saved += sample_digit((uv - float2(0.74, 0.80)) / cell, am % 10, 0);
+		color = lerp(color, saved.rgb, saturate(saved.a));
+
+		// 0.10912 * 1.83288 = 0.20: compensate physical surface aspect ratio.
+		float2 alarm_uv = (uv - float2(0.855, 0.79)) / float2(0.10912, 0.20);
+		float alarm_mask = 0.0;
+		if (all(alarm_uv >= 0.0) && all(alarm_uv <= 1.0))
+			alarm_mask = s_he_alarm.Sample(smp_base, alarm_uv).a;
+		color = lerp(color, float3(1.0, 0.88, 0.55), saturate(alarm_mask));
+	}
 
 	float brightness = 1.f;
 	brightness *= calc_glitch_brightness_watch_ui(brightness);
