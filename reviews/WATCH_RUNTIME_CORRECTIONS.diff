--- deployed/gamedata/config/system.ltx
+++ source/gamedata/config/system.ltx
@@ -1254,6 +1254,8 @@
 unskippable_time = 3000
 
 [shader_params_export]
+m_he_watch_ring
+m_he_watch_alarm
 m_digiclock
 m_actor_watch_params
 m_actor_watch_params_2
--- deployed/gamedata/config/text/eng/ui_st_keybinding.xml
+++ source/gamedata/config/text/eng/ui_st_keybinding.xml
@@ -7,7 +7,7 @@
 <string id="kb_binoculars"><text>Binoculars</text></string>
 <string id="kb_buy_menu"><text>Flashlight Mode</text></string>
 <string id="kb_chat"><text>Use Antirad</text></string>
-<string id="kb_chat_team"><text>Watch Alarm (wheel: minutes, Shift+wheel: hours; press again to exit)</text></string>
+<string id="kb_chat_team"><text>Alarm clock</text></string>
 <string id="kb_console"><text>Console</text></string>
 <string id="kb_contacts"><text>PDA Contacts</text></string>
 <string id="kb_crouch"><text>Crouch</text></string>
--- deployed/gamedata/config/text/rus/ui_st_keybinding.xml
+++ source/gamedata/config/text/rus/ui_st_keybinding.xml
@@ -7,7 +7,7 @@
 <string id="kb_binoculars"><text>Бинокль</text></string>
 <string id="kb_buy_menu"><text>Присоединить/Отсоединить прицел [Подсумок]</text></string>
 <string id="kb_chat"><text>Использовать антирад</text></string>
-<string id="kb_chat_team"><text>Будильник на часах (колесо — минуты, Shift+колесо — часы; повторное нажатие — выход)</text></string>
+<string id="kb_chat_team"><text>Будильник</text></string>
 <string id="kb_console"><text>Консоль</text></string>
 <string id="kb_contacts"><text>Контакты в ПДА</text></string>
 <string id="kb_crouch"><text>Присесть</text></string>
--- deployed/gamedata/scripts/he_watch_ui.script
+++ source/gamedata/scripts/he_watch_ui.script
@@ -6,6 +6,9 @@
 -- also use a door, NPC or pickup. The alarm key toggles the editor.
 
 local USE_HOLD_MS = 600
+local alarm_down = false
+local opened_at = 0
+local alarm_owned = false
 
 local state = "closed"
 local session = 0
@@ -28,6 +31,12 @@
 	return vergas_watch.g_watch
 end
 
+local function pressed(dik)
+	if not dik or dik <= 0 then return false end
+	local v = key_state(dik)
+	return v == true or (type(v) == "number" and v ~= 0)
+end
+
 local function use_dik()
 	if not key_bindings or not bind_to_dik then return nil end
 	return bind_to_dik(key_bindings.kUSE)
@@ -36,13 +45,13 @@
 local function block_use()
 	if use_blocked then return end
 	use_blocked = true
-	pcall(function() level.block_action(key_bindings.kUSE) end)
+	level.block_action(key_bindings.kUSE)
 end
 
 local function unblock_use()
 	if not use_blocked then return end
 	use_blocked = false
-	pcall(function() level.unblock_action(key_bindings.kUSE) end)
+	level.unblock_action(key_bindings.kUSE)
 end
 
 local function reset_use_gesture()
@@ -86,22 +95,24 @@
 
 local function show_overlay()
 	release_overlay()
-	pcall(function()
+	local ok, err = pcall(function()
 		local s = CUIStatic()
 		s:Init(192, 680, 640, 48)
 		s:SetFont(GetFontLetterica18Russian())
-		s:SetText(help_text())
+		s:SetTextComplexMode(true)
+		s:SetText((string.gsub(help_text(), "\\n", "\n")))
 		s:SetTextColor(255, 238, 153, 26)
-		s:SetTextComplexMode(true)
 		s:Show(true)
 		overlay = s
 		get_hud():AddDialogToRender(s)
 	end)
+	if not ok then log2("[he_watch] hint failed: %s", tostring(err)) end
 end
 
 local function local_cleanup()
 	release_overlay()
-	unblock_use()
+	-- Keep Use suppressed until a held disable key is physically released.
+	if not actor_ok() or not pressed(use_dik()) then unblock_use() end
 	reset_use_gesture()
 end
 
@@ -127,7 +138,7 @@
 	if state ~= "editing" then return end
 	local dir = 1
 	if vol and vol < 0 then dir = -1 end
-	if key_state(DIK_keys.DIK_LSHIFT) ~= 0 then
+	if pressed(DIK_keys.DIK_LSHIFT) or pressed(DIK_keys.DIK_RSHIFT) then
 		apply_draft_hours(dir)
 	else
 		apply_draft_minutes(dir)
@@ -135,6 +146,32 @@
 end
 
 local function on_update()
+	if use_blocked and state == "closed" and not pressed(use_dik()) then unblock_use() end
+	if not alarm_owned then
+		level.block_action(key_bindings.kPDA_CLOCK)
+		alarm_owned = true
+	end
+	local down_alarm = pressed(bind_to_dik(key_bindings.kPDA_CLOCK))
+	local rising_alarm = down_alarm and not alarm_down
+	alarm_down = down_alarm
+	if not actor_ok() then
+		abort("actor_unavailable")
+		return
+	end
+	if level.main_input_receiver() or device():is_paused() then
+		if is_active() then abort("menu_or_pause") end
+		return
+	end
+	if is_active() and (not watch_gate.has_watch() or db.actor:is_talking()) then
+		abort("watch_unavailable")
+		return
+	end
+	if state == "opening" and time_global() - opened_at > 5000 then
+		abort("entry_timeout")
+		log2("[he_watch] entry timed out; input restored")
+		return
+	end
+	if rising_alarm then toggle() end
 	if state ~= "editing" then
 		reset_use_gesture()
 		return
@@ -142,7 +179,7 @@
 	local dik = use_dik()
 	if not dik then return end
 	local now = time_global()
-	local down = key_state(dik) ~= 0
+	local down = pressed(dik)
 	if down then
 		if not use_down then
 			use_down = true
@@ -208,6 +245,7 @@
 local function open_editor(my_session)
 	if session ~= my_session or state ~= "opening" then return end
 	state = "editing"
+	log2("[he_watch] editor ready")
 	show_overlay()
 end
 
@@ -233,6 +271,7 @@
 	session = session + 1
 	local my_session = session
 	state = "opening"
+	opened_at = time_global()
 
 	local host = allcore_hide.begin_watch_presentation("alarm", function(host_session)
 		if session ~= my_session or state ~= "opening" then
@@ -285,7 +324,24 @@
 	end
 end
 
+local function start_input()
+	level.block_action(key_bindings.kPDA_CLOCK)
+	alarm_owned = true
+	alarm_down = pressed(bind_to_dik(key_bindings.kPDA_CLOCK))
+end
+
+local function stop_input()
+	abort("stop_modules")
+	unblock_use()
+	if alarm_owned then level.unblock_action(key_bindings.kPDA_CLOCK) end
+	alarm_owned = false
+	alarm_down = false
+end
+
 function attach(sm)
+	sm:subscribe({signal = "on_first_update", fun = start_input})
+	sm:subscribe({signal = "on_stop_modules", fun = stop_input})
+	sm:subscribe({signal = "on_load_before_all", fun = function() abort("load") end})
 	sm:subscribe({signal = "on_wheel_rotation", fun = on_wheel_rotation})
 	sm:subscribe({signal = "on_update", fun = on_update})
 end
--- deployed/gamedata/scripts/binders/bind_stalker.script
+++ source/gamedata/scripts/binders/bind_stalker.script
@@ -974,7 +974,7 @@
 	[key_bindings.kPDA_CLOCK] = {
 		func = function()
 			-- [HE-PORT] Watch-only alarm editor; the same key toggles open/cancel.
-			if he_watch_ui then he_watch_ui.toggle() end
+			-- he_watch_ui polls this action; never open the native PDA here.
 		end
 	},
 -- [HE-PORT]
--- deployed/gamedata/shaders/r3/model_clock.ps
+++ source/gamedata/shaders/r3/model_clock.ps
@@ -69,7 +69,7 @@
 	digit_offset[4] = float2(0.0, 0.0); //Colon
 
 	//Calculate time (H1/H2/M1/M2)
-    int4 game_time = int4(m_digiclock.xyzw * 10.0);
+    int4 game_time = int4(floor(m_digiclock.xyzw * 10.0 + 0.5));
 
 	//Calculate some offsets for blowout...
 	int blowout_offset[5];
--- deployed/gamedata/shaders/r3/model_clock.s
+++ source/gamedata/shaders/r3/model_clock.s
@@ -1,4 +1,4 @@
-local tex_lut = "shaders\\watch\\lut_digi_color"
+local tex_lut = "shaders\\watch\\he_clock_digits"
 
 function normal   (shader, t_base, t_second, t_detail)
 	  shader:begin	("model_def_lplanes","model_clock")
